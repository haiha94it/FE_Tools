# URL site production — dùng cho canonical, sitemap, Open Graph (bắt buộc khi deploy)
NEXT_PUBLIC_SITE_URL=https://carevippro.chotnhanh.vn

# API chính (auth, user, upload...)
NEXT_PUBLIC_API_URL=https://testcare.chotnhanh.vn

# Care API — Zalo messenger, tài khoản Zalo, nhóm...
NEXT_PUBLIC_CARE_API_URL=https://testcare.chotnhanh.vn

# WebSocket — bắt buộc khi dev localhost (WS không chạy trên :3000)
NEXT_PUBLIC_WS=wss://testcare.chotnhanh.vn